#include "config.hpp"
// #include <ustl/core/internal/arch/x86.hpp>

// using namespace ustl;
// using namespace core;
// using namespace internal;

TEST(x86Instruction, cmpxchg)
{
    // int now = 0;
    // int flush = 0;
    // int expected = 0;
    // ASSERT_TRUE(compare_and_exchange(now, flush, expected));
}